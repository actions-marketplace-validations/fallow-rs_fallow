# Semantic clone conformance corpus

The files below are unmodified excerpts from
[`rafal-qa/embedding-benchmark`](https://github.com/rafal-qa/embedding-benchmark)
at commit `0290537e84752f8bd1dac11fe6417064d82e46f9`:

- `cases/different-algorithm/`
- `cases/refactored-structure/`
- `cases/renamed-identifiers/`
- `cases/same-identifiers/`
- `cases/same-boilerplate/`
- `cases/shared-fragments/`
- `cases/unrelated/`

They retain the upstream MIT license in `LICENSE.embedding-benchmark`.
`manifest.json` records every upstream path and content digest. The relationship
labels are Fallow's manually reviewed evaluation labels, not upstream claims
about safe refactoring or actionable duplication.

The deterministic vectors used by Rust unit tests are synthetic contract
fixtures. They are not model quality evidence and contain no source-derived
values.
